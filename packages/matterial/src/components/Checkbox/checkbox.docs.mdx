---
name: Checkbox
description: Display a Boolean value
---

## Usage

```jsx
import { Checkbox } from 'matterial'

function CheckboxExample() {
  return <Checkbox onClick={() => console.log('I got clicked!')}>Click Me</Checkbox>
}
```

## Example

<CheckboxExample />

```jsx
import { Checkbox, useForm } from 'matterial'

const FORM = {
  isDaddy: true,
  hasBoat: false,
}

function App() {
  const { form, handleChange } = useForm(FORM)

  return (
    <Form>
      <Checkbox
        name="isDaddy"
        checked={form.data.isDaddy}
        onChange={handleChange}
      >
        I'm a daddy
      </Checkbox>
      <Checkbox
        name="hasBoat"
        checked={form.data.hasBoat}
        onChange={handleChange}
      >
        I have a boat
      </Checkbox>
      <pre>
        <code>{JSON.stringify(form.data, null, 2)}</code>
      </pre>
    </Form>
  )
}
```

## Sizes

<CheckboxSizesExample />

## Colors

<CheckboxColorsExample />

## Disabled

<Container row>
  <Checkbox disabled>Disabled</Checkbox>
  <Checkbox disabled checked>Disabled</Checkbox>
</Container>

## Indeterminate

<CheckboxIndeterminateExample />

```jsx
export function CheckboxIndeterminateExample() {
  const [state, setState] = useState(null)

  return (
    <Checkbox
      checked={!!state}
      indeterminate={state === null}
      onChange={(_, checked) => setState(checked)}
    >
      Indeterminate
    </Checkbox>
    <Button
      variant="contained"
      disabled={state === null}
      onClick={() => setState(null)}
    >
      Reset
    </Button>
  )
}
```
